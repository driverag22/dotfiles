[preview]
# This forces Yazi to use ueberzugpp for images
image_filter = "lanczos3"
image_quality = 75
max_width = 1500
max_height = 1500

[opener]
edit = [
  { run = "${EDITOR:-vi} %s", desc = "$EDITOR", block = true },
]
ipe = [
  { run = "ipe %s", orphan = true, desc = "Ipe", block = false },
]
pdff = [
	{ run = 'zathura "$@"', orphan = true, desc = "Zathura", block = false },
	{ run = 'sioyek "$@"', orphan = true, desc = "Sioyek", block = false },
]
image = [
  { run = 'ristretto "$@"', orphan = true, desc = "Ristretto", block = false },
   { run = 'feh "$@"', orphan = true, desc = "feh" },
  { run = 'sxiv "$@"', orphan = true, desc = "sxiv" },
  { run = 'qiv "$@"', orphan = true, desc = "qiv" },
]

[open]
rules = [
	# pdf
  { mime = "application/pdf", use = [ "pdff" ] },
	# ipe
	{ url = "*.ipe", use = [ "ipe" ]},
	# Folder
	{ url = "*/", use = [ "edit", "open", "reveal" ] },
	# Text
	{ mime = "text/*", use = [ "edit", "reveal" ] },
	# Image
	{ mime = "image/*", use = [ "image", "reveal" ] },
	# Media
	{ mime = "{audio,video}/*", use = [ "play", "reveal" ] },
	# Archive
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", use = [ "extract", "reveal" ] },
	# JSON
	{ mime = "application/{json,ndjson}", use = [ "edit", "reveal" ] },
	{ mime = "*/javascript", use = [ "edit", "reveal" ] },
	# Empty file
	{ mime = "inode/empty", use = [ "edit", "reveal" ] },
	# Virtual file system
	{ mime = "vfs/{absent,stale}", use = "download" },
	# Fallback
	{ url = "*", use = [ "edit" ]},
]


[plugin]
